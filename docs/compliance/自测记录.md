# 合规自测记录

日期：2026-09-24  
环境：本地代码审查（Maven 编译受 JDK 环境限制未在本机跑通，需在 CI/服务器复测）

## Item1 iOS 虚拟支付

| 用例 | 预期 | 结果 |
|------|------|------|
| iOS + 电子书 createOrder | 600302 阻断 | 代码路径：`IosVirtualPayPolicyService.assertCanCreateOrder` |
| iOS + 实物 createOrder | 允许 | `evaluateProduct` 非 virtual |
| Android + 会员 createOrder | 允许 | 非 iOS 跳过 |
| iOS payOrder 已建虚拟单 | 阻断统一下单 | `PaymentServiceImpl.createWxPayOrder` |
| 商品详情 `?platform=ios` | canPurchase=false | `MpProductController` |
| 审计 | mp_compliance_audit_event 有记录 | `ComplianceAuditService` |

**需用户截图**：iOS 真机详情页拦截弹窗；Android 正常支付；管理端交易设置 iOS 区块。

## Item2 类目

| 用例 | 预期 | 结果 |
|------|------|------|
| reviewMode=true 隐藏 planet | API 403/模块关 | `FeatureModuleGuard` + `WeChatMiniComplianceService` |
| 后台保存类目 | GET/PUT `/admin/wechat/compliance` | 已实现 |

**需用户截图**：`/settings/wechat` 类目区（待 UI 补全 JSON 编辑）；提审包隐藏星球入口。

## Item3 内容安全

| 用例 | 预期 | 结果 |
|------|------|------|
| 发布含违规样例 | reject | v2 msgSecCheck |
| API 超时/无 openid | pending 人审，不发布 | fail-closed |
| 评论 risky | 拒绝 | `ContentInteractServiceImpl` |

**需用户截图**：后台审核队列 pending；发布被拒提示。

## Item4–7

- 表结构 V89–V92 已建；业务 UI/API 部分未完成，见《交付摘要-parent.md》。

## 官方测试向量

- 文本/图片样例以微信《文本内容安全识别》《多媒体内容安全识别》文档当日样例为准（**待确认**是否仍提供固定测试串）。
