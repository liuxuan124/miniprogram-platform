# UX/UI 优化建议

> 基于 2026-06-17 全量测试后的交互与视觉专项建议
> 已落地项标注「✅ 已实施」，待办项标注「⏳ 建议」

---

## 一、已实施的 UX/UI 改进（本轮）

### 1. ✅ 图标体系专业化
- **问题**：左侧菜单 22 项全用 emoji（📊📄🎨📱），跨平台渲染不一致，缺乏企业感
- **改进**：全部替换为 Element Plus 图标库（`Odometer/Document/Brush/Box/Money` 等），视觉统一、专业

### 2. ✅ 销售排行空态占位
- **问题**：TOP3 只有 1 条数据时，第 2/3 名位置留白
- **改进**：不足 3 条时渲染灰色"暂无数据"占位卡，保持布局稳定

### 3. ✅ 工作台「最近访问」替代「快捷入口」
- **问题**：原 6 个快捷入口与左侧菜单完全重复，价值低
- **改进**：改为基于 localStorage 的「最近访问」（去重 + 倒序 + 相对时间），提供个性化价值；无记录时显示空态引导

### 4. ✅ 登录页增加「忘记密码」
- **改进**：增加忘记密码入口，引导联系超管在「管理员账号」页重置

### 5. ✅ 财务环比显示优化
- **问题**：基数为 0 时显示 ↑100% 造成歧义
- **改进**：基数为 0 时显示「—」；净利润亏损时红色标注「（亏损）」

### 6. ✅ 同步来源文案本地化
- **改进**：`erp` → 「业务系统」、`manual` → 「手动录入」，避免误解

---

## 二、⏳ 建议 UX/UI 改进（待办）

### A. 信息架构与导航

| 编号 | 建议 | 价值 |
|------|------|------|
| UX-1 | **工作台「演示路径」区域考虑折叠/移除** | 7 步演示路径（进入后台→...→小程序预览）是开发期引导，正式上线后对真实用户无意义，且占用首屏黄金区域。建议默认折叠或仅 super_admin 可见 |
| UX-2 | **顶部 TagsView 增加右键关闭其他/关闭右侧** | 多页签管理是后台高频操作，目前只有关闭按钮，缺少批量关闭 |
| UX-3 | **面包屑与页面标题重复** | 每个页面顶部既有面包屑（财务管理/财务概览）又有 H1（财务概览），信息冗余。建议二选一或面包屑作为轻量导航 |

### B. 数据可视化与反馈

| 编号 | 建议 | 价值 |
|------|------|------|
| UX-4 | **财务概览统计卡增加"较上周/上月"时间维度切换** | 目前总收入/支出是"全部历史"，无法反映近期趋势。建议加时间维度 Tab（本月/本季/本年/全部） |
| UX-5 | **图表增加 loading 骨架屏** | 当前 `v-loading` 是整块遮罩，数据加载时区域空白突兀。建议改用骨架屏过渡 |
| UX-6 | **空数据状态统一组件** | 各页面空态文案/图标不一（"暂无数据"/"暂无可用时段"/"暂无订单"），建议封装 `<EmptyState>` 组件统一视觉 |

### C. 表单与交互

| 编号 | 建议 | 价值 |
|------|------|------|
| UX-7 | **搜索表单增加"展开/收起"高级筛选** | 商品/订单页筛选项多时占屏过大，建议默认显示常用 2-3 项，其余折叠 |
| UX-8 | **危险操作二次确认 + 操作原因记录** | 删除/退款审批已有确认框，但建议对"批量删除""财务记录删除"增加必填原因，写入操作日志 |
| UX-9 | **表格行 hover 显示快捷操作浮层** | 当前操作列固定占用 160px 宽度，建议 hover 时才浮现操作按钮，节省横向空间 |

### D. 视觉规范

| 编号 | 建议 | 价值 |
|------|------|------|
| UI-1 | **统一主色与语义色** | 当前主色散落（#1769ff / #2474ff / #409eff 混用），建议收敛到单一主色 + CSS 变量，便于主题切换 |
| UI-2 | **状态色统一** | 成功/警告/危险/信息色建议与 Element Plus 主题对齐（#67c23a/#e6a23c/#f56c6c/#909399），避免自定义色值漂移 |
| UI-3 | **卡片圆角与阴影统一** | 工作台卡片 `border-radius:8px`，财务卡 `12px`，登录卡 `12px`；建议统一为 8px（信息密度高）或 12px（轻量） |
| UI-4 | **字号层级规范化** | 标题层级目前随意（24px/22px/18px/16px 混用），建议建立 Type Scale：H1=24 / H2=18 / Body=14 / Caption=12 |
| UI-5 | **深色侧边栏与浅色内容区对比度优化** | 侧边栏 `#0d1422` 与内容区 `#f4f6fb` 对比强烈但略压抑，建议侧边栏改 `#1e293b` 或加渐变提升层次 |

### E. 移动端与响应式

| 编号 | 建议 | 价值 |
|------|------|------|
| RWD-1 | **1366px 以下侧边栏默认折叠** | 小屏下固定 220px 侧边栏挤压内容区，建议 < 1366px 时默认折叠为 72px 图标模式 |
| RWD-2 | **平板竖屏布局适配** | 目前 `@media (max-width:760px)` 才转单列，768-1024px 平板区间仍是 2 列挤压，建议增加 1024px 断点 |

### F. 可访问性 (A11y)

| 编号 | 建议 | 价值 |
|------|------|------|
| A11y-1 | **所有图标按钮补充 `aria-label`** | 折叠按钮、刷新按钮等纯图标控件缺少无障碍标签 |
| A11y-2 | **颜色对比度** | 部分 muted 文字（#cbd5e1 on #fff）对比度 < 4.5:1，不满足 WCAG AA，建议改 #94a3b8 |
| A11y-3 | **键盘导航** | 确认所有弹窗/抽屉支持 Esc 关闭、Tab 焦点陷阱 |

---

## 三、优先级建议

| 优先级 | 项 | 建议时间 |
|--------|-----|---------|
| 🔴 高 | UI-1 主色统一、UX-6 空态组件 | 签收后第 1 周 |
| 🟡 中 | UX-1 演示路径折叠、UI-4 字号层级、RWD-1 小屏折叠 | 签收后 2-4 周 |
| 🟢 低 | UX-2 TagsView 右键、A11y 系列、UI-5 侧边栏调色 | 后续迭代 |

---

## 附：微信支付接入说明（P0-3 遗留项闭环）

> 现状：下单后不拉起微信支付，走"顾问联系"降级。代码层 `_doPay` 完整且含 C1 防绕过。

**接入步骤：**

1. **微信商户平台**（pay.weixin.qq.com）
   - 申请微信支付商户号（需企业资质 + 小程序已认证）
   - 开通「JSAPI 支付」产品
   - 绑定小程序 AppID

2. **后台配置**（`/settings/basic`）
   - 填写：商户号、APIv3 密钥、证书序列号、API 证书（p12）
   - 确认支付回调地址（如 `https://api.zfculture.site/api/v1/mp/payment/notify`）

3. **代码切换**（`miniapp/pages/order-detail/order-detail.js`）
   - 将 `onPayTap`（当前弹"顾问联系"）改为调用 `_doPay`
   - `_doPay` 已实现：请求后端 `payOrder` → 拿到 prepay 参数 → `wx.requestPayment`

4. **后端确认**（`MpOrderController.payOrder`）
   - 确认返回字段：`timeStamp / nonceStr / package / signType / paySign`
   - 确认 `MpPaymentController` 的 notify 回调能正确处理支付成功通知

5. **测试**
   - 小程序体验版选 0.01 元商品下单
   - 验证拉起微信支付 → 支付成功 → 订单状态变 paid
   - 验证支付回调写入财务流水

**切换后 C1 自动通过**：`_doPay` 第 161 行已有"金额>0 且未显式标记已支付则不显示支付成功"的防绕过逻辑。
