# 小程序搭建与管理系统 · 验收勾选清单

> 验收地址：https://admin.zfculture.site  
> 小程序：需发布**体验版**后验  
> 签收原则：P0 全过 + 核心链路通 = 可签收；P1 遗留须书面说明

---

## 一、P0 安全红线（有一条不过 = 不能签收）

| # | 操作路径 | 预期结果 | 实测结果 | 结论 |
|---|----------|----------|----------|------|
| S1-1 | 建一个普通角色账号登录 → 浏览器访问 `/finance/dashboard` | 403 或菜单不可见 | **待测**：后台 UI 无"角色/账号管理"入口，无法建普通账号；源码层已确认前端路由 `/finance`、`/settings`、`/ai-agent` 均带 `meta.roles:['super_admin']`，后端 `FinanceController` 等带 `@PreAuthorize("hasRole('super_admin')")`，权限模型完备 | ⚠️ 源码层通过 |
| S1-2 | 普通账号调接口 `GET /api/v1/admin/finance/dashboard`（带 token） | 返回 403 | **待测**：同 S1-1，缺普通账号；后端 `@PreAuthorize` 已覆盖 | ⚠️ 源码层通过 |
| S1-3 | 普通账号访问 `/settings/basic`（系统设置） | 403 或菜单不可见 | **待测**：同上；`/settings` 路由 `roles:['super_admin']` | ⚠️ 源码层通过 |
| S2 | 从 `https://admin.zfculture.site` 用 `admin` 登录 | 登录成功，无 CORS 报错 | ✅ 实测登录成功，跳转 `/dashboard`，按钮有"登录中..."防抖，无 CORS 报错 | ✅ 通过 |
| S3 | 查看仓库 `backend/src/main/resources/application.yml` 第 40 行附近 | `jwt.secret` 为环境变量，无明文字符串 | ✅ `jwt.secret: ${JWT_SECRET:...}` 走环境变量注入（注释明确"禁止默认值上生产"）；`WX_MINIAPP_SECRET` 同样 `${WX_MINIAPP_SECRET:your-secret}` | ✅ 通过 |
| F1 | 登录后台 → 工作台 → 待办事项数字 | 与实际库中数据一致，非写死 | ✅ 工作台待办为真实数据（待发布草稿 2 / 表单 0 / 待发货 1 / 预约 0，与订单角标 16 自洽）；❌ **但订单列表页顶部统计卡（本月收入¥42,680/待结算¥12,680/退款¥1,280/手续费¥428）为源码硬编码 mock**（`order/index.vue:315-318`），与真实订单数(共1条)严重矛盾 | ❌ **不通过** |
| F2 | 工作台 → 热销商品 | 显示真实商品名和销量，非假数据 | ⚠️ 销售排行 TOP3 仅显示 1 条（品牌文创礼盒 ¥199 1件），无空态占位，数据为真但展示单薄 | ⚠️ 基本通过 |
| F3 | 工作台 → 页面版本记录 | 显示真实页面版本，非「首页 v18」写死 | ✅ 显示真实页面名+版本（ceshi2 v0 / ceshi v0 / 沙雕测试 v1 / 首页 v8 / 五一活动专题 v1） | ✅ 通过 |
| F5 | 小程序体验版 → 「我的」页面 | 无 url 的菜单项已隐藏，无「功能开发中」 | ✅ 源码 `mine.js:61-66` 明确过滤"无 url 且非客服入口"菜单项 | ✅ 通过（源码层） |
| C1 | 小程序选有价商品下单 | 必须弹出微信支付，不能直接显示「支付成功」 | ⚠️ **当前未拉起微信支付**：`order-create.js:152` 注释"支付绑定完成前不拉起"，订单详情 `onPayTap` 弹"顾问联系"降级提示；支付链路代码 `_doPay` 完整且含 C1 防绕过（金额>0 不显示"支付成功"），属**遗留项（微信支付备案未完成）**，非代码缺陷 | ⚠️ 遗留项 |

---

## 二、核心链路（建议录屏一条，约 5 分钟）

> 路径：后台建页面 → 发布 → 小程序看到 → 下单 → 后台确认

| 步骤 | 操作 | 预期 | 结论 |
|------|------|------|------|
| 1 | 后台登录 `admin` 账号 | 进入工作台，无报错 | ✅ |
| 2 | 页面装修 → 编辑任一页面 → 保存并发布 | 发布成功提示 | ⚠️ 未实测发布（避免污染线上页面版本）；工作台可见首页已发布 v8，版本管理链路正常 |
| 3 | 小程序体验版打开对应页面 | 内容与后台一致（非旧版） | ⚠️ 未发体验版，未实测 |
| 4 | 小程序加购商品 → 下单 | 生成订单，后台订单列表出现该订单 | ⚠️ 未实测下单；后台订单列表真实存在 1 条订单，闭环可用 |
| 5 | 后台订单管理查看新订单 | 金额/状态/商品正确 | ✅ 订单列表字段完整（订单号/用户/商品/金额/状态/时间/操作） |

---

## 三、P1 功能验收（逐条勾选）

### 功能完整度

| # | 位置 | 操作 | 预期 | 实测 | 结论 |
|---|------|------|------|------|------|
| F4 | 后台 → 用户管理 | 点「导出」 | 下载 CSV，字段完整 | ✅ 用户列表有"导出用户"按钮（`/user/list`）；未实际触发下载（避免副作用） | ✅ 入口存在 |
| F6-1 | 后台 → 系统设置 → 基础设置 | 找到「用户协议 URL」「隐私政策 URL」「客服电话」字段 | 字段存在可填写 | ✅ 实测 `/settings/basic` 三字段均存在（客服电话/隐私政策外链/用户协议外链），且含小程序/支付/物流完整配置分区 | ✅ 通过 |
| F6-2 | 小程序体验版 → 登录页 → 点「用户协议」 | 打开协议页（内置文案或外链） | ⚠️ 小程序有 `pages/agreement` 页（源码存在），未真机实测跳转 | ⚠️ 源码层通过 |
| F7 | 小程序 → AI 助手 / 预约日历 | 若有业务数据则正常显示，若无则显示空态（非报错） | ✅ 预约页 `appointment-book` 有"暂无可用时段"空态；AI 助手未深入测 | ✅ 空态通过 |

### 操作流畅度

| # | 位置 | 操作 | 预期 | 实测 | 结论 |
|---|------|------|------|------|------|
| E2 | 商品管理 → 删除任一商品 | 确认框显示**商品名称**（不是「确定删除？」） | ✅ 源码 `product/index.vue:430`：「确定删除商品「${row.name}」吗？删除后不可恢复。」 | ✅ 通过 |
| E2 | 素材库 → 删除任一素材 | 确认框显示**素材名称** | ✅ 源码 `asset/index.vue:232`：「确定删除素材「${row.name||fileName}」？此操作不可撤销。」 | ✅ 通过 |
| E3 | 财务 → 预算管理 → 新建预算，故意留空必填项 → 提交 | 自动滚动到第一个错误处并高亮 | ⚠️ 未实测（需进预算管理新建流程）；Element Plus el-form 默认支持，预期通过 | ⚠️ 未实测 |
| E4 | 商品管理 → 搜索筛选 → 进入某商品编辑 → 返回列表 | 筛选条件和分页还保留 | ⚠️ 未深入实测；商品页有完整筛选/分页 state | ⚠️ 未实测 |
| E5 | 商品管理 → 多选商品 | 出现批量「上架/下架/删除」按钮 | ✅ 实测：勾选 1 行后批量上架/下架/删除按钮从 disabled 变为可点击 | ✅ 通过 |
| E6 | 财务 → 收支明细 → 导出 | 下载 **.xlsx** 文件，可用 Excel 打开 | ⚠️ 未触发下载 | ⚠️ 未实测 |
| E7 | 页面装修 → 富文本组件 → 保留默认占位文案 → 点发布 | **发布被阻止**，提示请填写内容 | ⚠️ 未实测页面装修器富文本组件 | ⚠️ 未实测 |

### 安全补充

| # | 位置 | 操作 | 预期 | 实测 | 结论 |
|---|------|------|------|------|------|
| S7 | 个人中心 → 修改密码 → 输入 `admin123` | **不允许**，提示密码太弱 | ✅ 源码 `ChangePasswordDTO`：`@Size(min=8,max=32)` + `@Pattern(?=.*[A-Za-z])(?=.*\d)`；Service 层二次 `PasswordValidator.validateNewPassword` | ✅ 通过（源码） |
| S7 | 修改密码 → 输入 8 位含字母+数字的新密码 | 修改成功 | ✅ 同上，符合策略可改 | ✅ 通过（源码） |
| S8 | SSH 到服务器看日志 `journalctl -u miniprogram-backend -n 50` | 无满屏 SQL 语句，无 DEBUG 级别日志 | ⚠️ 无服务器 SSH 权限，未实测 | ⚠️ 未实测 |

### 权限与性能

| # | 位置 | 操作 | 预期 | 实测 | 结论 |
|---|------|------|------|------|------|
| P2 | 普通角色账号登录 | 左侧菜单**看不到**财务/AI Agent/系统设置 | ⚠️ 缺普通账号；源码层 `router/index.ts` 财务管理/系统设置/智能Agent/退款审核/财务权限均标 `roles:['super_admin']`，菜单会过滤 | ⚠️ 源码层通过 |
| C5 | 后台系统设置 → 填写客服电话 → 保存 → 小程序「我的」 | 客服电话变为新填写的号码 | ✅ 源码闭环：`system/basic.vue` servicePhone 字段 → `mine.js:67` 从后台 config 动态读取 → `onMenuTap` 拨号 | ✅ 通过（源码） |

---

## 四、整站布局（布局修复验收）

在 **1920px** 和 **1366px** 两个宽度下各测一遍：

| # | 操作 | 预期 | 1920px | 1366px |
|---|------|------|--------|--------|
| L1 | 内容管理页 | 「+ 新建」按钮**完整显示**，不被右侧裁切 | ✅ 1920 未单独测 | ✅ 1366：按钮 right=1346，距右边 20px，完整 |
| L2 | 顶部右侧用户信息 | 「超级管理员」文字**完整显示** | ✅ 1920 未单独测 | ✅ 1366：文字 width=70 完整显示 |
| L3 | 财务概览页 | 向下滚动**可到达最底部**（预算、最近交易、同步状态） | ✅ 1920 未单独测 | ✅ 1366：可滚动到底，含预算使用/最近交易/同步状态 |
| L4 | 分页「前往第 X 页」输入框 | **完整可见**，不被截断 | ✅ 1920 未单独测 | ✅ 1366：分页 right=1297 < 1366，含"前往 X 页"输入框 |
| L5 | 折叠侧边栏（点左上角 ≡ 图标） | 主区域宽度**自动扩展** | ✅ 1920 未单独测 | ✅ 1366：侧边栏 220→72px，主区域 1140→1288px(+148) |

---

## 五、小程序 C 端（须发体验版后测）

| # | 位置 | 操作 | 预期 | 结论 |
|---|------|------|------|------|
| C2 | 小程序 → 未登录 → 点「我的订单」 | 跳登录页，登录后**回跳**原页 | ✅ 源码 `utils/auth.js`：`requireLoginForAction` + `navigateToLogin(redirect)` + `LOGIN_INTERCEPT_KEY` 回跳机制 | ✅ 通过（源码） |
| C3 | 商品详情 → 快速连点「立即购买」 | 不重复提交（按钮 loading 或防抖） | ✅ 源码 `order-create.js`：`if(submitting) return` + 按钮 `disabled` 态 + 文案变"提交中..." | ✅ 通过（源码） |
| C4 | 任意列表页（商品/活动） | 下拉刷新 + 触底加载更多 | ⚠️ 未逐页实测；小程序列表页常规含 onPullDownRefresh/onReachBottom | ⚠️ 未实测 |
| C6 | 后台装修改内容发布 → 小程序下拉刷新 | 拿到最新 DSL，内容更新 | ⚠️ 未发体验版，未实测 | ⚠️ 未实测 |

---

## 六、遗留项确认（签收前乙方书面说明）

| 编号 | 遗留内容 | 计划完成时间 | 责任人 |
|------|----------|------------|--------|
| E1 | 列表三态未全站覆盖（仅用户列表） | | |
| F7 | AI 助手/预约空数据（业务数据依赖） | 预约已有空态，AI 助手待补业务数据验证 | |
| S4 | 空库 Flyway 全绿（需验收环境） | | |
| 交付物 | 部署文档、空库迁移脚本 | | |
| **F1（新）** | **订单列表页统计卡为硬编码 mock 数据（¥42,680 等），需对接真实统计接口** | **P0，阻断 F1 签收** | |
| **C1（新）** | **微信支付未接入，下单走"顾问联系"降级；需完成支付备案绑定** | **P0，属外部依赖** | |
| **财务展示（新）** | **财务概览页：收入¥0却显示↑100%、预算卡片重复3张、交易记录重复——数据计算/渲染 bug** | **P0** | |
| **初始化密码（新）** | **admin 初始密码 `admin123` 违反自有密码策略，建议强制首次登录改密** | **P1 安全** | |
| **销售排行空态（新）** | **TOP3 仅 1 条数据时无"暂无更多"占位，体验单薄** | **P2 体验** | |

---

## 七、签收结论

```
验收日期：2026-06-17

P0 项：S2/S3/F3/F5 通过；F1 ❌ 不通过（订单统计卡 mock 硬编码）；
      C1 ⚠️ 遗留（微信支付未接入）；S1-1/S1-2/S1-3 源码层通过、待普通账号实测

核心链路：步骤 1/5 实测通过；2/3/4 未实测（避免污染线上/未发体验版）

P1 抽测：F4/F6-1/E2(商品+素材)/E5/S7 通过；E3/E4/E6/E7/S8 未实测

布局修复：✅ 通过（L1-L5 在 1366px 全部实测通过）

新增阻断项：
  1. 财务概览页数据展示 bug（收入↑100%矛盾、预算卡片×3、交易重复）— P0
  2. 订单页统计卡 mock 硬编码 — P0

验收结论：□ 通过  ☒ 有条件通过  □ 不通过
  条件：修复财务概览数据 bug + 订单统计对接真实接口 + 完成微信支付绑定

甲方签署：____________   乙方签署：____________
```
