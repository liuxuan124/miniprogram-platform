# 交接提示词：继续「微信小程序多场景搭建与运营系统」UI/UX 与搭建发布链路优化

> 把本文件内容整段贴给你在其它软件上使用的 AI，作为任务上下文。

---

## 项目背景

这是一个微信小程序多场景搭建与运营系统，包含三部分：`admin`（Vue3 + TypeScript + Element Plus 管理后台）、`miniapp`（微信小程序原生前端）、`backend`（Java Spring Boot）。核心能力是页面搭建器（后台拖拽装修，小程序端按 DSL 动态渲染），配套电商、内容、会员、活动、预约、AI Agent 等模块。

此前已完成一轮全面 UI/UX 优化，分三批推进，全部已落地并通过 `vue-tsc` 类型检查与 `sass` 样式编译校验（零新增错误）。现在需要你接手继续做**长尾优化项**和**尚未覆盖的批量铺开工作**。

---

## 已完成的工作（不要重复做，但要复用已建立的模式）

### 设计系统地基
- `admin/src/assets/styles/index.scss` 已建立完整 token 体系：颜色（`--brand` `--success` `--warning` `--danger` 等）、间距 `--space-1`~`--space-6`、圆角三档 `--radius-sm/--radius/--radius-lg`、阴影 `--shadow-sm/md/lg`、字号阶梯 `--font-h1/h2/h3/body/sm/caption`。新页面/新组件必须用这些 token，不要再写死 hex 色值或裸 px。
- 全局样式里有语义化排版类：`.page-title` `.section-title` `.card-title` `.text-muted` `.text-secondary` `.text-danger` 等，以及 `.mt-1`~`.mt-6` 这类间距工具类。

### 已激活的公共组件（之前是零引用的死代码，现在已铺开，继续沿用）
- `admin/src/components/ListStateWrap.vue`：管理后台列表页三态包裹（loading 骨架屏 / error / empty），已接入 `product/index.vue`、`order/index.vue`、`user/index.vue`、`content/index.vue`。**还有大量列表页没接入**，见下方「未完成」。
- `admin/src/components/EmptyState.vue`：配合 `ListStateWrap` 使用，也可单独用。
- `admin/src/components/PageHeader.vue`：页面标题+操作区，已用于商品/工作台等页，未全站铺开。
- `miniapp/components/skeleton/skeleton.*`：小程序端骨架屏组件，支持 `type="home"|"grid"|"list"`，已接入 `pages/index`、`pages/product-list`、`pages/content-list`、`pages/order-list`。
- `miniapp/components/empty-state/empty-state.*`：小程序端统一空态组件，同上四个页面已接入，其余页面未接入。

### 页面搭建器（装修器）
- `admin/src/stores/page.ts`：已实现撤销/重做历史栈（`canUndo` `canRedo` `undo()` `redo()`），结构性操作单独入栈，props/style 连续输入用 500ms 窗口合并成一条历史。
- `admin/src/components/page-builder/CanvasArea.vue`：已支持拖拽落点指示线、画布内组件拖拽换位、75%/100%/125% 缩放、Delete 删除选中、Ctrl+D 复制选中。
- `admin/src/components/page-builder/ComponentPanel.vue`：已加搜索框和"最近使用"分组（localStorage 持久化）。
- `admin/src/components/page-builder/PropsPanel.vue`：四向边距已合并为联动控件（可锁定等比），"通用样式"默认折叠。
- `admin/src/views/page-builder/editor.vue`：已实现自动保存（30 秒静默存草稿）、保存冲突顶部提示条（可选"放弃修改刷新"或"另存为新草稿"）、发布成功结果面板（版本号/组件数/生效路径）。
- `admin/src/components/page-builder/componentRegistry.ts`：新增了 `isRenderSupportedByMiniapp()` 渲染端能力校验——发布前会检查组件类型小程序端是否真的有渲染实现，不支持则阻断发布。**这份支持清单必须与 `miniapp/utils/render.js` 里的 `COMPONENT_TYPES` 手动保持同步**，新增组件类型时三处都要改（admin 注册表 + miniapp 白名单 + miniapp 渲染分支），否则会重演之前 `form_entry` 组件小程序端白屏的问题（已修复）。

### 已修复的真实 bug（顺手发现的，不是主动排查出来的，说明代码里可能还有类似问题）
- `miniapp/components/dsl-renderer/` 缺少 `form_entry` 渲染分支，导致后台配置了表单入口组件、正常发布后小程序端白屏。已补上。
- `miniapp/utils/render.js` 的 `COMPONENT_TYPES` 白名单漏了 `image` 和 `form_entry`。已补上。
- `admin/src/views/dashboard/index.vue` 里 `dashboardLoading` 被 `v-loading` 和空态判断引用了 5 处，但从未声明为 ref，加载态从来没生效过。已修复。
- `admin/src/layout/TagsView.vue` 缺少右键菜单；`admin/src/stores/app.ts` 里 `closeOtherVisitedViews`/`closeAllVisitedViews` 早就写好但零调用。已补上右键菜单并激活，还新增了 `closeRightVisitedViews`。

**建议你在动手前先花时间通读代码，而不是直接照单摩改**——这个项目里不止一次出现"组件/方法已经写好但零引用"的情况，说明之前的开发节奏是功能堆得快、集成跟不上。你应该假设类似的死代码或断链还有更多，先审查再动手。

---

## 尚未完成的工作（按优先级）

### 1. A2 色值替换：还有大量文件未处理
第一轮只处理了硬编码色值密度最高的 5 个文件（miniapp-builder.vue、product/edit.vue、page-builder/index.vue、asset/index.vue、PersonalCenterCustomizer.vue），且**只做了模板+样式层的安全映射**（跳过了所有 `<script>` 块和含 `gradient` 的行，避免误伤 echarts 这类需要真实色值的场景）。全站硬编码色值原本有 2057 处，这一轮大概处理了 190 处左右。运行这个命令看当前剩余情况：
```bash
cd admin/src && grep -roE "#[0-9a-fA-F]{3,6}" --include=*.vue . | wc -l
```
继续按密度排序处理其余文件。**方法论**：写脚本做映射替换（hex → `var(--token)`），只映射语义明确的颜色（品牌蓝、语义色、灰阶文字、边框、浅色背景），跳过渐变行和 `<script>` 块，替换完用 `vue-tsc --noEmit` 和 `sass` CLI 逐个校验，不要肉眼看着"应该没问题"就跳过验证。

### 2. A5 骨架屏/三态组件继续铺开
`ListStateWrap` 目前只接了 4 个管理后台列表页。还有：商品分类、素材库、会员、优惠券、活动、预约、表单提交、财务各表、AI 会话记录、操作日志等列表页。小程序端 `skeleton`/`empty-state` 也还有购物车、活动列表、预约列表、优惠券列表、积分记录、我的预约等页面没接入（用 `grep -rln "empty-state\|暂无" pages/ --include=*.wxml` 能找到候选清单）。

### 3. A9 可访问性（长尾，之前没做）
- 所有纯图标按钮（折叠、刷新、导出、列设置）补 `aria-label`。
- `muted` 文字色（当前 `#cbd5e1` 系）检查对比度，不满足 WCAG AA 4.5:1 的调整到接近 `#94a3b8`（token 里已有 `--text-muted`，检查有没有地方还在用更浅的旧值）。
- 确认所有弹窗/抽屉支持 Esc 关闭、Tab 焦点陷阱（Element Plus 组件大部分默认支持，重点检查自定义弹层，比如 `TagsView.vue` 新加的右键菜单——目前只有点击遮罩关闭，建议加 Esc 监听，参考 `editor.vue` 里 `handleKeydown` 的写法）。

### 4. C4 版本管理体验（长尾，之前没做）
`admin/src/views/page-builder/version.vue` / `version-management.vue`：版本列表目前只有版本号和时间，按方案要求增加"与当前版本对比"（按组件粒度展示新增/删除/修改），回滚前展示该版本画布预览缩略图。这个工作量较大，需要后端配合返回版本间 diff 或至少返回每个版本的完整 DSL 供前端比对，建议先看后端 `PageController`/`PageVersionController` 现有接口能力，不要假设数据现成。

### 5. D4 小程序端交互细节（长尾，之前没做）
- 列表页下拉刷新/触底加载的统一反馈（目前样式分散）。
- 关键操作（加购、领券、提交预约）补 loading 与成功态动画。
- 图片统一 lazy-load 与失败占位（部分页面已有 `lazy-load` 属性，检查失败占位 `binderror` 是否都处理了）。

### 6. D3 微信支付切换（待确认，非纯技术工作）
`miniapp/pages/order-detail/order-detail.js` 的 `onPayTap` 目前弹"顾问联系"降级，`_doPay` 已实现完整。切换依赖微信支付商户号资质是否办妥，需要先跟业务方确认，不要自行切换生产支付逻辑。

---

## 工作方式要求（照抄了会踩坑的教训）

1. **改之前先读**：这个代码库里已经出现过好几次"功能写了但没接上"的情况，别假设文档或注释描述的就是现状，用 grep/read 验证。
2. **每次改动后必须跑校验**：`cd admin && npx vue-tsc --noEmit -p tsconfig.json`（全量跑一次记下已有的 4 个预置错误——`dashboard/index.vue` 和 `setup-guide.vue` 里的 `pageSize` 参数名不匹配、`setup-guide.vue` 两处类型推断问题——这些是本轮优化前就存在的，不是你引入的，不用因为看到报错就慌，但也别在这几个文件上继续叠加新错误）。样式改动额外跑 `sass` CLI 编译对应 `<style>` 块，命令模式：
   ```bash
   awk '/<style/{flag=1; next} /<\/style>/{flag=0} flag' 目标文件.vue > /tmp/x.scss
   npx sass --no-source-map /tmp/x.scss /tmp/x.css
   ```
3. **色值替换脚本要跳过 `<script>` 块和 `gradient` 行**，原因见上文"已完成"部分，不要图省事整文件无差别替换。
4. **小程序端改动优先检查三处联动**：admin 组件注册表 / miniapp 渲染白名单 / miniapp 渲染分支，三处不同步就是下一个 `form_entry` 式白屏 bug。
5. **本沙箱环境的 `vite`/`rollup` 二进制架构不匹配，`npm run build` 跑不起来**，这是环境问题不是代码问题，用 `vue-tsc --noEmit` 和 `sass` CLI 分别校验类型和样式即可，不必纠结跑不起来完整构建。

---

## 关键文件速查

| 用途 | 路径 |
|------|------|
| 设计令牌 | `admin/src/assets/styles/index.scss` |
| 搭建器状态 | `admin/src/stores/page.ts` |
| 搭建器三栏 | `admin/src/components/page-builder/{ComponentPanel,CanvasArea,PropsPanel}.vue` |
| 搭建器主页（发布/自动保存/冲突处理） | `admin/src/views/page-builder/editor.vue` |
| 组件注册表 + 渲染端支持清单 | `admin/src/components/page-builder/componentRegistry.ts` |
| 三态/空态组件 | `admin/src/components/{ListStateWrap,EmptyState,PageHeader}.vue` |
| 小程序动态渲染器 | `miniapp/components/dsl-renderer/dsl-renderer.{wxml,wxss,js}` |
| 小程序渲染白名单 | `miniapp/utils/render.js` |
| 小程序骨架屏/空态组件 | `miniapp/components/{skeleton,empty-state}/` |
| 全量方案文档（含优先级批次划分） | `全面优化方案-UIUX与搭建发布链路.md` |

---

把这份文档贴给新环境的 AI 后，建议先让它做一次全项目 `vue-tsc` 跑通确认基线，再开始动手，避免把预置的 4 个错误误认为自己引入的。
