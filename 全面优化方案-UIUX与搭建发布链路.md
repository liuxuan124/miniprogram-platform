# 全面优化方案：UI/UX 与搭建—发布—应用全链路

> 制定日期：2026-07-22　|　基线：master 分支现有代码
> 范围：管理后台 UI/UX 全站、页面搭建器、发布流程、小程序端 C 端体验
> 本文档为方案，确认后再进入实施

---

## 一、代码审查发现的核心问题

本轮直接通读了 `admin/src`（144 个 vue 文件）、`stores/page.ts`、`components/page-builder/*`、`miniapp/components/dsl-renderer/*` 与 `miniapp/pages/index/*`，发现的问题与此前 `UX-UI优化建议.md` 有较大出入，需要先纠正三个认知：

**认知一：设计令牌已经建好了，但几乎没人用。** `assets/styles/index.scss` 已经定义了完整的一套 token（`--brand` `--success` `--radius` `--font-h1` 等 20 余个变量），并且已经桥接到 Element Plus 主题变量。但全站 144 个 vue 文件中，只有 5 个文件真正引用了 `var(--brand...)`，其余散落着 **2057 处硬编码色值**，其中 `#1769ff` 出现 122 次、`#409eff` 出现 32 次。所以 UI-1「统一主色」的问题不是"缺规范"，而是"规范没落地"——这是一次机械替换工作，价值高、风险低。

**认知二：公共组件是死代码。** `components/EmptyState.vue` 和 `components/ListStateWrap.vue` 都已经写好，但全站**零处引用**。`整改对照表.md` 中 E1 标记的「`ListStateWrap` + 用户列表三态」实际并未接入任何页面。这意味着 UX-6「空态统一组件」的工作量比预估小得多——组件已在，只差铺开。

**认知三：搭建器的短板不在视觉，在编辑器基础能力。** 装修器三栏结构（`ComponentPanel` / `CanvasArea` / `PropsPanel`）完整，27 种组件的 props 面板与渲染器齐备，发布前占位校验也已生效。真正缺失的是编辑器的"肌肉记忆"能力：没有撤销重做、没有画布内拖拽排序、没有落点指示、没有自动保存、没有快捷键。这些不是锦上添花，是运营人员每天要用几十次的东西。

此外发现一个**功能性缺口**：admin 侧注册了 `form_entry`（表单入口）组件，有完整的 props 面板和后台渲染器，但 `miniapp/components/dsl-renderer/` 中没有任何 `form_entry` 分支。运营人员在后台拖入表单入口组件、正常保存发布后，小程序端会渲染出空白——这是一个静默失败，且发布前校验不会拦截。

---

## 二、优化方案分四条主线

### 主线 A：管理后台 UI/UX 全面升级

这条主线解决"看起来不像一个产品"的问题。审查量化出的失控程度如下，可作为改造前后的对比基线：

| 维度 | 现状 | 目标 |
|------|------|------|
| 硬编码色值 | 2057 处，主色变体 4 种（#1769ff ×122、#409eff ×32、#3b82f6 ×3） | 全部走 token，主色唯一 |
| token 采用率 | 144 个 vue 中仅 5 个引用 `var(--brand)` | 高频页面 100% |
| 圆角 | 8 种并存（8px ×89、12px ×55、10px ×55、6px ×34、4px ×19、14px ×12…） | 收敛为 3 档 |
| 字号 | 12 种并存（12px ×185、13px ×155、11px ×79、14px ×69…） | Type Scale 6 档 |
| 空态 | 32 个文件各写各的"暂无…" | 统一组件 |
| 加载态 | 45 处 `v-loading` 整块遮罩 | 骨架屏 |
| 列表页 | 38 个 `el-table` 页面，三态处理不一致 | 统一 `ListStateWrap` |

**A1　补齐设计令牌体系。** 现有 token 覆盖了颜色和字号，但缺三组关键的：间距 `--space-1`~`--space-6`（4/8/12/16/24/32px）、阴影 `--shadow-sm/md/lg`、层级 `--z-dropdown/modal/toast`。同时把 `--radius` 拆为三档：`--radius-sm: 6px`（标签、按钮）、`--radius: 8px`（卡片、输入框）、`--radius-lg: 12px`（对话框、大面板），把现有 8 种圆角收敛过去。字号建立完整 Type Scale：`--font-h1: 22px` / `--font-h2: 18px` / `--font-h3: 16px` / `--font-body: 14px` / `--font-sm: 13px` / `--font-caption: 12px`，并明确 11px 及以下一律上调到 12px（当前有 79 处 11px、16 处 10px，在 1080p 屏上已接近不可读）。

**A2　全站替换硬编码色值。** 按密度排序分三轮推进：第一轮处理搭建器与高频运营页（`miniapp-builder.vue` 141 处、`PersonalCenterCustomizer.vue` 128 处、`product/edit.vue` 101 处、`page-builder/index.vue` 60 处、`asset/index.vue` 62 处），第二轮处理订单/财务/内容/会员各模块，第三轮扫尾。做法是先建一张色值→token 的映射表（主色族、语义色族、中性灰阶、边框色），用脚本批量替换后人工复核每个 diff；装饰性渐变、图表配色、品牌插画色不动，避免误伤。每轮替换后跑一次视觉回归（关键页面截图对比）。

**A3　建立语义化排版类。** 光有 token 不够——现在页面里到处直接写 `font-size: 18px; font-weight: 700; color: #172033`。抽出 `.page-title` `.section-title` `.card-title` `.text-secondary` `.text-muted` `.text-danger` 等语义类放进全局样式，页面改为写类名。这样后续调整层级只需改一处，也让新页面天然一致。

**A4　激活两个死代码组件并铺开。** `EmptyState.vue` 已写好且正确使用了 token，但零引用；`ListStateWrap.vue` 同样零引用且功能不完整。先给 `ListStateWrap` 补齐 loading / empty / error 三态、retry 事件与骨架屏插槽，再铺到全部 38 个列表页（商品、订单、内容、活动、会员、素材、优惠券、预约、表单提交、财务各表、AI 会话、日志）。同步把 32 个文件里手写的"暂无…"替换为 `EmptyState`，每处补上有意义的引导文案和行动按钮（例如商品列表空态给"去添加第一个商品"而不是干巴巴的"暂无数据"）。

**A5　加载体验：骨架屏替代遮罩。** 45 处 `v-loading` 整块遮罩在数据返回前是一片空白，视觉跳变明显。列表页改为表格骨架（保持行高与列宽），卡片区改为卡片骨架，图表区改为图表轮廓骨架。首屏感知速度会明显改善。

**A6　导航与信息架构收敛。** 面包屑与页面 H1 信息重复，建议保留 `PageHeader` 组件统一承载标题、副标题与右侧操作区（目前只有 6 个文件在用，需要铺到全部业务页），面包屑降级为侧边栏当前项高亮。工作台的 7 步「演示路径」是开发期引导，占用首屏黄金区域，改为默认折叠或仅 super_admin 可见。TagsView 补右键菜单（关闭其他 / 关闭右侧 / 关闭全部 / 刷新当前）。侧边栏当前 hover 态用的是 `rgba(23,105,255,0.14)` 硬编码，随 A2 一并 token 化。

**A7　表格与表单交互。** 列表页操作列固定占 160px，改为 hover 时浮现操作按钮，把横向空间还给数据。筛选表单项多时默认只显示常用 2-3 项，其余折叠进"高级筛选"。表格支持列显隐配置与密度切换（宽松/紧凑），运营人员在笔记本上能多看几行。危险操作（批量删除、财务记录删除）在现有二次确认基础上增加必填原因并写入操作日志。

**A8　响应式。** 视口宽度小于 1366px 时侧边栏默认折叠为 72px 图标模式；补 1024px 断点，解决 768–1024px 平板竖屏区间仍是两列挤压的问题；宽表格在窄屏下改为横向滚动而非压缩列宽。

**A9　可访问性。** 所有纯图标按钮（折叠、刷新、导出、列设置）补 `aria-label`；muted 文字从 `#cbd5e1` 调到 `#94a3b8` 以满足 WCAG AA 4.5:1 对比度；确认所有弹窗与抽屉支持 Esc 关闭与 Tab 焦点陷阱；表单校验错误同时用颜色和图标+文字表达，不依赖单一色觉。

### 主线 B：搭建器编辑体验重做

这条主线解决"运营人员用得累"的问题，是本轮价值最高的部分。

**B1　撤销 / 重做。** 在 `stores/page.ts` 中引入历史栈：每次 `addComponent` / `removeComponent` / `moveComponent` / `updateComponentProps` / `updateComponentStyle` 前后打快照（对 props 的连续输入做 500ms 防抖合并，避免一个字一条历史）。栈深上限 50。工具栏加撤销/重做按钮，绑定 `Ctrl+Z` / `Ctrl+Shift+Z`。这是当前最痛的缺口——现在删错一个组件只能手动重建。

**B2　画布内拖拽排序与落点指示。** 现在 `CanvasArea` 的 `handleDrop` 只接收从左侧组件库拖来的新组件，且一律 `push` 到末尾；画布内已有组件只能靠 `BaseRenderer` 上的上移/下移按钮一格一格挪。改造为：左侧拖入时根据鼠标 Y 坐标计算插入位置并显示一条蓝色落点线，落下时调用已有的 `addComponent(type, index)`（store 已支持 index 参数，只是没人传）；画布内组件支持按住拖拽换位，复用 `moveComponent`。这一项的收益与工作量比最高。

**B3　自动保存草稿。** 当前只有手动「保存草稿」，`isDirty` 状态下刷新浏览器就丢。改为 `isDirty` 变化后 30 秒静默存草稿，工具栏显示"已自动保存 HH:mm"。注意与现有的乐观锁（`expectedVersion`）配合，冲突时不要弹窗打断，改为顶部常驻提示条。

**B4　组件面板检索与分组优化。** 27 个组件目前平铺，加搜索框与最近使用分组。组件项加悬停缩略预览。

**B5　画布可用性。** 增加缩放档位（75% / 100% / 125%）与"隐藏辅助线"开关，方便在小屏笔记本上装修；选中组件时属性面板自动滚动到对应区块；支持 `Delete` 键删除选中组件、`Ctrl+D` 复制。

**B6　属性面板结构化。** 现在「组件属性」与「通用样式」两段扁平铺开，边距四项各占一行。改为四向边距合并为一个联动控件（可锁定等比），并把「通用样式」默认折叠。

### 主线 C：发布链路可靠性

**C1　补齐 form_entry 的小程序端渲染。** 在 `dsl-renderer.wxml` / `.wxss` / `.js` 中补 `form_entry` 分支，跳转到已有的 `miniapp/pages/form`。这是修 bug，优先级最高。

**C2　发布前校验升级为"渲染端能力校验"。** 现有 `validateComponent` 只校验 props 是否填了占位内容，不校验该组件类型小程序端是否支持。增加一份"渲染端支持清单"，发布前比对，发现小程序端不支持的组件类型直接阻断并给出明确提示。这能从机制上防止 C1 这类问题再次出现。

**C3　发布流程反馈优化。** 当前发布成功后弹一个确认框引导去配置小程序，逻辑正确但信息密度高。改为发布成功后展示一个结果面板：本次发布的版本号、变更组件数、小程序端生效路径、下一步建议（含"去配置导航"按钮和二维码预览入口）。

**C4　版本管理体验。** 版本列表增加"与当前版本对比"——按组件粒度展示新增/删除/修改，而不是只给一个版本号和时间。回滚前展示该版本的画布预览缩略图。

**C5　保存冲突处理。** 现在冲突时只提示"请刷新页面后重新编辑"，用户的编辑内容直接丢失。改为提供"保留我的修改另存为新草稿"选项。

### 主线 D：小程序端 C 端体验

**D1　骨架屏替代整页 loading。** `pages/index` 目前加载期整页只有一个 `loading` 组件。改为按 DSL 结构渲染骨架（banner 占位块 + 列表占位卡），首屏感知速度会明显改善。同样铺到商品列表、内容列表、订单列表。

**D2　空态与错误态统一。** 现有 `empty-state` 样式各页重复定义，抽为公共组件，统一图标、文案与"重试/去逛逛"行动按钮。

**D3　下单支付链路。** 按 `UX-UI优化建议.md` 附录的五步接入微信支付，把 `order-detail.js` 的 `onPayTap` 从"顾问联系"降级切回 `_doPay`。这一项依赖商户号资质，属于配置阻塞项，需要你确认资质进度。

**D4　交互细节。** 列表页加下拉刷新与触底加载的统一反馈；关键操作（加购、领券、提交预约）补 loading 与成功态动画；图片统一 lazy-load 与失败占位。

---

## 三、实施优先级与批次

UI/UX 升级与功能修复并行推进，不排在功能之后——因为 A1/A3/A4 属于地基类工作，越早做，后续所有页面改动越省事；若先做功能再回头统一样式，等于把同一批文件改两遍。

| 批次 | 内容 | 理由 |
|------|------|------|
| **第一批（地基 + 阻断缺陷）** | A1 令牌体系、A3 语义排版类、A4 三态与空态组件激活；C1 form_entry 渲染修复、C2 渲染端能力校验 | A1/A3/A4 是后续所有 UI 工作的前置依赖，必须最先落；C1/C2 是会导致发布后白屏的功能性缺陷，不能拖 |
| **第二批（视觉铺开 + 搭建器核心）** | A2 全站色值替换（三轮）、A5 骨架屏、A6 导航收敛；B1 撤销重做、B2 画布拖拽排序与落点指示 | A2/A5/A6 让全站视觉真正统一；B1/B2 是运营人员每天几十次的高频痛点，收益/成本比最高 |
| **第三批（交互打磨）** | A7 表格与表单交互、A8 响应式；B3 自动保存、B4~B6 面板与画布优化；C3 发布结果反馈、C5 冲突处理；D1 小程序骨架屏、D2 空态统一 | 体验提升明确，但不阻塞日常使用 |
| **第四批（长尾）** | A9 可访问性、C4 版本对比、D4 交互细节 | 可随迭代推进 |
| **待你确认** | D3 微信支付切换 | 依赖商户号资质，非纯技术工作 |

**关于视觉风格确认**：第一批做完 A1/A3 后，我会先改造 2 个样板页（建议工作台 + 商品列表，一个偏展示一个偏数据）给你确认风格，你点头后再进第二批的全站铺开。这样避免 2057 处改完才发现方向不对。

---

## 四、需要你确认的三件事

第一，**样板页选哪两个**。默认建议工作台 + 商品列表；如果你更关心搭建器，也可以换成装修器 + 页面列表。

第二，**A2 全站替换 2057 处色值是否一次做完**。一次做完最干净，但改动覆盖 100+ 文件，需要一轮完整回归测试；也可以按方案里的三轮分批，先做搭建器与高频运营页，其余随迭代扫尾。

第三，**D3 微信支付**的商户号资质目前进展到哪一步，决定它是排进本轮还是挂起。

---

## 附：本方案引用的关键文件

搭建器状态：`admin/src/stores/page.ts`
搭建器三栏：`admin/src/components/page-builder/{ComponentPanel,CanvasArea,PropsPanel}.vue`
编辑器主页：`admin/src/views/page-builder/editor.vue`
设计令牌：`admin/src/assets/styles/index.scss`
未使用的公共组件：`admin/src/components/{EmptyState,ListStateWrap}.vue`
小程序渲染器：`miniapp/components/dsl-renderer/dsl-renderer.{wxml,wxss,js}`
小程序首页：`miniapp/pages/index/index.{js,wxml}`
页面 API：`admin/src/api/page.ts`
