<view class="empty">
  <view class="empty__title">{{title}}</view>
  <view wx:if="{{desc}}" class="empty__desc">{{desc}}</view>
  <view wx:if="{{buttonText}}" class="empty__btn" bindtap="onAction">{{buttonText}}</view>
</view>
