# 小程序搭建与管理系统 · 全面测试与建议报告

> 测试时间：2026-06-17
> 测试人：以甲方/真实用户视角
> 测试方式：浏览器真机走查（admin/admin123）+ 源码静态审查 + 小程序源码审查
> 验收地址：https://admin.zfculture.site

---

## 一、总体结论

**验收结论：有条件通过**

平台整体完成度高、架构清晰、核心链路闭环可用，**布局修复全部通过**，权限模型与安全设计在源码层完备。但存在 **2 个 P0 阻断项**和 **1 个 P0 遗留项**，须修复后方可签收。

| 级别 | 数量 | 性质 |
|------|------|------|
| 🔴 P0 阻断 | 2 | 必须修复才能签收 |
| 🟡 P0 遗留 | 1 | 外部依赖（微信支付备案） |
| 🟠 P1 重要 | 4 | 建议签收前修复 |
| 🔵 P2 优化 | 6 | 体验/工程改进 |

---

## 二、🔴 P0 阻断项（必须修复）

### P0-1：订单列表页统计卡为硬编码 mock 数据

- **位置**：`admin/src/views/order/index.vue:315-318`
- **现象**：订单列表顶部 4 张统计卡（本月收入 ¥42,680 / 待结算 ¥12,680 / 本月退款 ¥1,280 / 平台手续费 ¥428）为源码**写死的常量**，与真实订单数（共 1 条）严重矛盾
- **证据**：
```315:318:admin/src/views/order/index.vue
  { title: '本月收入', value: '¥42,680', note: '较上月 +18.2%', up: true, ... },
  { title: '待结算', value: '¥12,680', note: '预计3个工作日到账', ... },
  { title: '本月退款', value: '¥1,280', note: '退款率 3.0%', ... },
  { title: '平台手续费', value: '¥428', note: '微信支付费率 0.6%', ... },
```
- **影响**：违反验收清单 F1（数据须真实，非写死），甲方无法基于此做经营决策
- **建议**：对接后端统计接口（如 `GET /api/v1/admin/order/statistics`），按"本月/上月"真实聚合，空数据时显示 ¥0 + "暂无数据"

### P0-2：财务概览页数据展示 bug（多处）

- **位置**：`/finance/dashboard`
- **现象**（实测截图可见）：
  1. **"收入 ¥0.00，↑100.0%"** — 收入为 0 却显示 ↑100%，逻辑自相矛盾
  2. **"总支出 ¥5,998.50，↑0.0%"** — 有支出却显示 ↑0%
  3. **"净利润 ↑100.0%"** — 净利润实为 -5998.5（亏损），箭头方向错误
  4. **"支出分类：暂无数据"** — 但明细区有 5998.5 支出，分类聚合失效
  5. **"2026运营预算 ¥0/¥100000 0%" 卡片重复出现 3 张** — 疑似 v-for 循环渲染 bug
  6. **交易明细"测试收入 +¥1999.50"重复出现多次** — 数据去重/分页 bug
- **影响**：财务数据是甲方最敏感的关注点，此类错误会直接动摇对系统的信任
- **建议**：
  - 环比百分比计算需处理"基数为 0"的边界（基数为 0 时应显示"—"而非 ↑100%）
  - 净利润箭头须按"利润增加↑/亏损扩大↓"正确判定
  - 预算卡片检查 v-for 的 key 绑定，避免重复渲染
  - 交易明细检查后端 SQL 去重 + 前端分页

---

## 三、🟡 P0 遗留项（外部依赖，非代码缺陷）

### P0-3：微信支付未接入（C1）

- **现状**：
  - `miniapp/pages/order-create/order-create.js:152` 注释"支付绑定完成前不自动拉起微信支付"
  - `order-detail.js` 的 `onPayTap` 用弹窗"顾问会联系你确认付款"降级
  - **但 `_doPay` 支付链路代码完整**，含 `wx.requestPayment` 全套参数 + C1 防绕过逻辑（金额>0 且未显式标记已支付则不显示"支付成功"）
- **结论**：代码层面**不会**绕过支付直接显示成功（符合 C1 安全要求）；属微信支付备案/绑定未完成的外部遗留
- **建议**：完成微信支付商户号绑定后，将 `onPayTap` 切换为 `_doPay`，并在订单创建后自动拉起支付

---

## 四、🟠 P1 重要项（建议签收前修复）

### P1-1：admin 初始密码违反自有密码策略

- **现象**：admin 初始密码为 `admin123`，而系统 `ChangePasswordDTO` 要求"8-32位且含字母+数字"（admin123 仅符合长度，强度极弱）
- **风险**：初始弱密码 + 超级管理员权限 = 高危
- **建议**：强制首次登录改密；或初始化即生成随机强密码通过安全渠道下发

### P1-2：无法在 UI 创建普通角色账号（影响 S1/P2 实测）

- **现象**：后台左侧菜单无"角色管理/管理员账号"入口（路由表也无相关页面），仅 `RoleController`/`AdminUserController` 后端存在
- **影响**：甲方无法自助创建普通角色账号做权限验收；也阻碍了"普通账号访问财务返 403"的实测
- **建议**：补全管理员账号管理 UI（列表/新建/分配角色），否则权限体系对甲方不可操作

### P1-3：销售排行 TOP3 无空态占位

- **位置**：工作台
- **现象**：TOP3 榜单只有 1 条数据（品牌文创礼盒），第 2/3 名位置留白，无"暂无更多"提示
- **建议**：不足 3 条时显示灰色占位卡"暂无更多商品"

### P1-4：菜单 emoji 图标过度依赖，缺少正式图标体系

- **现象**：左侧菜单 22 项全部用 emoji（📊📄🎨📱📝📋👑👥🛍️📦🎁🎪📅💰🧾🔐🤖🖼️⚙️），不同操作系统渲染不一致（Win/Mac/Linux 显示差异大），且不符合企业级后台的专业感
- **建议**：替换为 Element Plus 图标库或 SVG 图标，统一视觉规范

---

## 五、🔵 P2 体验与工程优化

### P2-1：财务概览页"手动同步"来源显示 `erp`

- 同步来源显示 "erp"，但系统是小程序平台，来源命名易引起误解，建议改为 "本系统" 或可配置

### P2-2：路由命名不统一

- `/finance/dashboard` vs `/order/list` vs `/commerce/product` vs `/user/list` — 资源层级深浅不一
- `/content` 实际跳到"文章管理"（而非"内容管理"首页），命名与落地页不符
- 建议：统一为 `/{module}/{resource}` 两级结构

### P2-3：订单页日期筛选器常驻日历

- 实测发现订单页"下单时间"选择器展开后，日历表格（"日一二三四五六 31123456..."）混入了订单数据抓取逻辑，DOM 结构略乱
- 建议：检查 el-date-picker 的 popper 渲染层级

### P2-4：登录页缺少"忘记密码"入口

- 企业级后台应有"忘记密码"自助找回或管理员重置提示

### P2-5：工作台"快捷功能入口"与左侧菜单重复

- 工作台的 6 个快捷入口（装修首页/内容发布/表单构建/会员体系/商品上架/营销工具）与左侧菜单完全重叠，价值有限
- 建议：改为"最近访问"或"我的收藏"，提供个性化价值

### P2-6：build 产物与临时脚本污染仓库根目录

- **现象**：仓库根目录有 50+ 个 `.tmp-*.py` 脚本、`build*.txt`、`err*.txt`、`effective_pom.txt`、大量 `screen-*.png` 等
- **建议**：加入 `.gitignore` 并清理，保持仓库整洁（当前 git status 已显示这些是 untracked，但应明确忽略）

---

## 六、✅ 表现良好的方面（值得肯定）

| 维度 | 表现 |
|------|------|
| **登录安全** | 登录按钮有"登录中..."防抖态，防止重复提交 |
| **删除确认** | 商品/素材/分类删除均带具体名称（"确定删除商品「XX」吗？删除后不可恢复"），体验专业 |
| **批量操作** | 商品管理批量上架/下架/删除，勾选后按钮自动启用 |
| **密码策略** | 改密接口双层校验（DTO 注解 + PasswordValidator），8-32位含字母数字 |
| **权限模型** | 前端路由 `roles` 过滤 + 后端 `@PreAuthorize` 双重防护 |
| **密钥管理** | JWT/WX Secret 全部走环境变量注入，无硬编码 |
| **小程序菜单** | 我的页过滤无 url 死入口，避免"功能开发中" |
| **支付防绕过** | 金额>0 且未显式标记已支付时，不显示"支付成功" |
| **防重复提交** | 订单创建有 submitting 锁，按钮 disabled |
| **登录回跳** | `requireLoginForAction` + redirect 机制完整 |
| **布局响应** | 侧边栏折叠 220→72px，主区域自动 +148px 扩展 |
| **客服电话闭环** | 后台配置 → 小程序动态读取 → 拨号，链路完整 |
| **空态处理** | 预约页"暂无可用时段"空态规范 |

---

## 七、未实测项说明

以下项因条件限制未完成实测，建议甲方在乙方配合下补充验证：

| 项 | 原因 | 补测建议 |
|----|------|----------|
| S1-1/S1-2/S1-3/P2 普通账号权限 | 后台无创建普通账号入口 | 乙方手动建普通账号或开放账号管理 UI |
| 核心链路步骤 2-4（页面发布/小程序下单） | 避免污染线上页面版本 + 未发体验版 | 乙方发体验版后录屏 5 分钟走查 |
| E3 预算表单错误高亮 | 未深入预算新建流程 | 手动新建预算留空测试 |
| E4 筛选保留 | 未深入往返测试 | 商品页筛选→编辑→返回验证 |
| E6 收支导出 xlsx | 未触发下载 | 实际导出验证 Excel 可打开 |
| E7 富文本发布阻止 | 未进页面装修器 | 装修器内测富文本组件 |
| C4 下拉刷新/触底加载 | 未发体验版 | 体验版实测 |
| C6 DSL 热更新 | 未发体验版 | 体验版实测 |
| S8 服务器日志级别 | 无 SSH 权限 | 乙方提供日志样本 |

---

## 八、修复优先级建议

```
立即修复（阻断签收）：
  1. P0-1 订单统计卡对接真实接口
  2. P0-2 财务概览数据计算 bug（4 处）

签收前修复（强烈建议）：
  3. P1-1 admin 初始密码强化
  4. P1-2 补全账号管理 UI（否则权限无法验收）

签收后迭代：
  5. P0-3 微信支付绑定（外部依赖）
  6. P1-3/P1-4 空态 + 图标体系
  7. P2-* 体验优化
```

---

*报告生成于 2026-06-17，基于 admin 账号浏览器实测 + 全栈源码审查。*
