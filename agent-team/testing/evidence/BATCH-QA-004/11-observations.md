# BATCH-QA-004 观测摘录

Playwright `screenshot` 仍可能因等字体超时失败；本批以会话内 a11y snapshot 与 `innerText` 摘录为证据。未发布「出海笔记首页」。页面 id=2「五一活动专题」草稿被测到 v3（分享标题写入「QA离线保存探测」，`pull_refresh=true`）。

## 小程序端

见 `00-miniapp-attempt.md`。`cli islogin=true`，`open` / `auto --trust-project` 成功，AppID `wxea3928e0978492fe`，IDE HTTP `127.0.0.1:53097`。前台进程列表仍无「微信开发者工具」窗口名；`9420` 无监听。主线二下单、主线三提交仍无模拟器证据，维持 BLOCKED。

## 装修器 · 下拉刷新（页面 id=2 五一活动专题）

- 全局配置「下拉刷新」可打开，开关变为 `[checked]`，顶部「未保存」后自动保存。
- 预览对话框「小程序端实时预览 · 真实数据」展示活动入口卡片；在预览区域 `mousewheel(0,-300)` 后快照无「刷新中」或下拉指示。
- `GET /api/v1/admin/pages/2`：`statusDesc=草稿`，`pull_refresh=true`。
- 真实小程序手势未执行。

## 装修器 · 断网保存

- 分享标题改为「QA离线保存探测」后设 browser offline，点「保存草稿」。
- Toast：「网络异常，请检查网络连接」；顶部「未保存」「自动保存失败」。
- 控制台两次 `ERR_INTERNET_DISCONNECTED` → `POST/PUT /api/v1/admin/pages/2/draft`。
- 恢复 online 后自动保存成功：`v3`「已自动保存 16:38」；接口 `share_title=QA离线保存探测`。
- 成功后错误 toast 仍挡住「保存草稿」点击，见 BUG-UI-006。

## 页面模板 `/page-builder/template-center`

- 标题「页面模板」，筛选项含分类/行业/场景/风格。
- 「共 10 套模板」，含电商增长首页、活动裂变专题、品牌馆展示及多个行业首页；均标 P0。未点「使用模板」（避免新建页）。

## 素材库 `/asset/list`

- 共 2 个：首页活动 Banner（图片 100.0 KB）、品牌礼盒主图（图片 200.0 KB）。
- 计数：全部 2，图片 **0**，视频/音频/图文 0；分组「首页装修素材 **0**」。
- 接口两条 `type=image` `groupId=1`。点「图片」筛选后列表仍是这 2 条，计数仍为 0。见 BUG-ASSET-001。

## 系统设置 `/settings/basic`

- 小程序名称「品牌小程序」，AppID `wxea3928e0978492fe`，分享引导语「出海笔记 · 阿哲｜选品·供应链·独立站」20/50。
- 微信支付开关文案「未启用 / 已启用」（未改开关）。
- 权限角色页签：超级管理员（系统内置）、内容运营、商务运营。未新建角色、未改权限。
- 管理员账号仅 `admin` 一条；角色列乱码 `è¶…çº§ç®¡ç†å‘˜`。见 BUG-SET-001。角色矩阵实操仍仅超管，EXP-ROLE-MATRIX 维持 BLOCKED。

## 智能 Agent `/ai/agent`

- 「AI Agent 配置中心」六步：模型接入 → Prompt → 知识库 → 沙盒 → 发布 → 监控。
- 当前「已选用」GPT-5.4；未点「测试连接」、未保存密钥。

## 会员等级 / 积分

- 等级 Lv.1～4 均启用，**会员数全 0**；积分日志 **0 条**。对照 BATCH-QA-003 会员列表张小明金卡/1280 分。见 BUG-MEMBER-002。

## 财务子页

- 收支明细：暂无数据。
- 财务报表：总收入/总支出/净利润/日均收入均为 ¥0.00。与已支付种子订单对照，沿用 BUG-FIN-001，本批不新开财务缺陷。
