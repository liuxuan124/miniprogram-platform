# BATCH-QA-012 全量组件属性实测

工作模式：QA。未发布首页。证据目录本文件同级。

页面属性：PASS（名称可改，下拉刷新可点）。无分享封面。
通用样式：PASS（边距锁定、上边距步进可点）。无 padding、无 visible。
26 个组件均可添加。分类导航/资质证书默认 items=[]，子项字段要先「添加」才出现。悬浮按钮默认动作=打开客服，故不显示页面路径。

详见 prop-matrix.json 与各组件截图。
