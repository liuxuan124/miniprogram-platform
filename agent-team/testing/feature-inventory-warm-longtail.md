# 暖阁小程序冒烟 + 长尾专项功能点清单

> 批次：`BATCH-QA-WARM-LT-001`  
> 体验版目标：`1.30.4`  
> 范围性质：**范围扩展附录**（不计入装修器正式台账 268；禁止无意义全量重跑）  
> 总控批准：本批 = 标准流程最小集 + 长尾专项 A–E（用户已确认，免枚举门禁）

## 覆盖率（本附录分母）

| 项 | 值 |
| --- | --- |
| 本批功能点数 | **12**（FP-WARM-001 ~ FP-WARM-012） |
| 正式台账 268 | 不重跑、不改历史 PASS 结论 |

## 八维度图例

每 FP 下表：`适用` / `不适用(+理由)`。未覆盖的适用维度 → 该 FP 最高只能 `PARTIAL`。

---

### FP-WARM-001 登录后资料可见

| 维度 | 适用性 |
| --- | --- |
| 正常路径 | 适用 |
| 边界值 | 不适用（展示可读即可） |
| 异常输入 | 不适用 |
| 权限越权 | 适用（未登录应引导登录/访客态） |
| 幂等与重复 | 不适用 |
| 并发 | 不适用 |
| 时序 | 适用（登录后重进我的页） |
| 数据一致性 | 适用（与 profile / mine overview 一致） |

### FP-WARM-002 设置改昵称保存

| 维度 | 适用性 |
| --- | --- |
| 正常路径 | 适用 |
| 边界值 | 适用（空昵称、超长 10 字） |
| 异常输入 | 适用（安全检测未通过） |
| 权限越权 | 适用（未登录） |
| 幂等与重复 | 适用（连点保存） |
| 并发 | 不适用 |
| 时序 | 适用（保存后重进） |
| 数据一致性 | 适用（DB / 本地缓存） |

### FP-WARM-003 设置换头像：upload + profile + 重进仍在（禁 wxfile 入库）

| 维度 | 适用性 |
| --- | --- |
| 正常路径 | 适用 |
| 边界值 | 适用（取消选图、上传失败） |
| 异常输入 | 适用（临时路径不得入库） |
| 权限越权 | 适用 |
| 幂等与重复 | 适用（Android onShow 竞态） |
| 并发 | 不适用 |
| 时序 | 适用（选图→保存→杀进程重进） |
| 数据一致性 | 适用（`mp_user.avatar_url` 无 wxfile） |

### FP-WARM-004 我的页头像 binderror / 默认图

| 维度 | 适用性 |
| --- | --- |
| 正常路径 | 适用（坏链 → PNG 默认） |
| 边界值 | 适用（已是默认图不再切换） |
| 异常输入 | 不适用 |
| 权限越权 | 不适用 |
| 幂等与重复 | 适用（多次 error） |
| 并发 | 不适用 |
| 时序 | 不适用 |
| 数据一致性 | 不适用（仅展示层） |

### FP-WARM-005 客服发图持久化重进可见

| 维度 | 适用性 |
| --- | --- |
| 正常路径 | 适用 |
| 边界值 | 适用（上传失败 toast） |
| 异常输入 | 适用（拒绝非持久 URL） |
| 权限越权 | 适用 |
| 幂等与重复 | 不适用 |
| 并发 | 不适用 |
| 时序 | 适用（重进会话） |
| 数据一致性 | 适用（消息存远程 URL） |

### FP-WARM-006 表单/投稿选图上传（isPersistedMediaUrl）

| 维度 | 适用性 |
| --- | --- |
| 正常路径 | 适用 |
| 边界值 | 适用（上传失败） |
| 异常输入 | 适用（临时路径过滤） |
| 权限越权 | 适用 |
| 幂等与重复 | 不适用 |
| 并发 | 不适用 |
| 时序 | 适用（草稿不落 wxfile） |
| 数据一致性 | 适用 |

### FP-WARM-007 评价图提交前上传

| 维度 | 适用性 |
| --- | --- |
| 正常路径 | 适用 |
| 边界值 | 适用（最多 4 张、上传失败） |
| 异常输入 | 适用 |
| 权限越权 | 适用 |
| 幂等与重复 | 适用（防重复提交） |
| 并发 | 不适用 |
| 时序 | 适用（先 upload 再 submit） |
| 数据一致性 | 适用 |

### FP-WARM-008 无封面商品/收藏 PNG 占位不裂

| 维度 | 适用性 |
| --- | --- |
| 正常路径 | 适用 |
| 边界值 | 适用（空 cover） |
| 异常输入 | 不适用 |
| 权限越权 | 不适用 |
| 幂等与重复 | 不适用 |
| 并发 | 不适用 |
| 时序 | 不适用 |
| 数据一致性 | 不适用（包内资源） |

### FP-WARM-009 签到已签再点有 toast

| 维度 | 适用性 |
| --- | --- |
| 正常路径 | 适用 |
| 边界值 | 不适用 |
| 异常输入 | 不适用 |
| 权限越权 | 适用 |
| 幂等与重复 | 适用（已签重复点击） |
| 并发 | 不适用 |
| 时序 | 不适用 |
| 数据一致性 | 适用（与签到状态接口一致） |

### FP-WARM-010 平台/星球会员双态字段接口（mine overview / planet home）

| 维度 | 适用性 |
| --- | --- |
| 正常路径 | 适用 |
| 边界值 | 适用（双态均为 false） |
| 异常输入 | 不适用 |
| 权限越权 | 适用（mine 需登录） |
| 幂等与重复 | 不适用 |
| 并发 | 不适用 |
| 时序 | 不适用 |
| 数据一致性 | 适用（字段与订购表语义一致） |

### FP-WARM-011 上传接口与 /uploads/ 拉图 200

| 维度 | 适用性 |
| --- | --- |
| 正常路径 | 适用 |
| 边界值 | 不适用（本批聚焦可达性） |
| 异常输入 | 不适用 |
| 权限越权 | 适用（未登录 upload） |
| 幂等与重复 | 不适用 |
| 并发 | 不适用 |
| 时序 | 不适用 |
| 数据一致性 | 适用（磁盘文件可经公网拉取） |

### FP-WARM-012 弱网/失败有提示

| 维度 | 适用性 |
| --- | --- |
| 正常路径 | 不适用（本 FP 专测失败态） |
| 边界值 | 不适用 |
| 异常输入 | 适用 |
| 权限越权 | 不适用 |
| 幂等与重复 | 不适用 |
| 并发 | 不适用 |
| 时序 | 适用（弱网超时） |
| 数据一致性 | 不适用 |
